# ==============================================================================
# app.R — Shiny 主程序
# ==============================================================================

# 加载全局设置 (库, 辅助函数, 业务逻辑)
source("global.R")

# 加载 UI 定义
source("R/ui.R")

# 加载 Server 逻辑
source("R/server.R")

# 启动应用
shinyApp(ui = ui, server = server)
